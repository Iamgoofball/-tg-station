/obj/effect/cloning
    name = "cloning"
    desc = "Cloning is no longer available."

    // Remove cloning functionality
    proc/Initialize()
        ..()
        del(self)

    // Remove cloning procedures
    proc/Clone(target)
        // Cloning is no longer available
        return

    // Remove cloning effects
    proc/Effect()
        // Cloning is no longer available
        return

/obj/item/cloning_pod
    name = "cloning pod"
    desc = "This cloning pod is no longer functional."

    // Remove cloning pod functionality
    proc/Initialize()
        ..()
        del(self)

    // Remove cloning pod procedures
    proc/Clone(target)
        // Cloning is no longer functional
        return

    // Remove cloning pod effects
    proc/Effect()
        // Cloning is no longer functional
        return

/obj/item/cloning_scanner
    name = "cloning scanner"
    desc = "This cloning scanner is no longer functional."

    // Remove cloning scanner functionality
    proc/Initialize()
        ..()
        del(self)

    // Remove cloning scanner procedures
    proc/Scan(target)
        // Cloning scanning is no longer functional
        return

    // Remove cloning scanner effects
    proc/Effect()
        // Cloning scanning is no longer functional
        return

/obj/item/cloning_console
    name = "cloning console"
    desc = "This cloning console is no longer functional."

    // Remove cloning console functionality
    proc/Initialize()
        ..()
        del(self)

    // Remove cloning console procedures
    proc/Clone(target)
        // Cloning is no longer functional
        return

    // Remove cloning console effects
    proc/Effect()
        // Cloning is no longer functional
        return

/obj/item/cloning_dna_scanner
    name = "cloning DNA scanner"
    desc = "This cloning DNA scanner is no longer functional."

    // Remove cloning DNA scanner functionality
    proc/Initialize()
        ..()
        del(self)

    // Remove cloning DNA scanner procedures
    proc/Scan(target)
        // Cloning DNA scanning is no longer functional
        return

    // Remove cloning DNA scanner effects
    proc/Effect()
        // Cloning DNA scanning is no longer functional
        return

/obj/item/cloning_console
    name = "cloning console"
    desc = "This cloning console is no longer functional."

    // Remove cloning console functionality
    proc/Initialize()
        ..()
        del(self)

    // Remove cloning console procedures
    proc/Clone(target)
        // Cloning is no longer functional
        return

    // Remove cloning console effects
    proc/Effect()
        // Cloning is no longer functional
        return

/obj/item/cloning_dna_scanner
    name = "cloning DNA scanner"
    desc = "This cloning DNA scanner is no longer functional."

    // Remove cloning DNA scanner functionality
    proc/Initialize()
        ..()
        del(self)

    // Remove cloning DNA scanner procedures
    proc/Scan(target)
        // Cloning DNA scanning is no longer functional
        return

    // Remove cloning DNA scanner effects
    proc/Effect()
        // Cloning DNA scanning is no longer functional
        return

/obj/item/cloning_console
    name = "cloning console"
    desc = "This cloning console is no longer functional."

    // Remove cloning console functionality
    proc/Initialize()
        ..()
        del(self)

    // Remove cloning console procedures
    proc/Clone(target)
        // Cloning is no longer functional
        return

    // Remove cloning console effects
    proc/Effect()
        // Cloning is no longer functional
        return

/obj/item/cloning_dna_scanner
    name = "cloning DNA scanner"
    desc = "This cloning DNA scanner is no longer functional."

    // Remove cloning DNA scanner functionality
    proc/Initialize()
        ..()
        del(self)

    // Remove cloning DNA scanner procedures
    proc/Scan(target)
        // Cloning DNA scanning is no longer functional
        return

    // Remove cloning DNA scanner effects
    proc/Effect()
        // Cloning DNA scanning is no longer functional
        return

[FILE: code/modules/character.dm]