/obj/nanite_ui
    name = "nanite UI"
    desc = "A user interface for programming nanites."

    proc/Open()
        // Open the UI

    proc/Close()
        // Close the UI

    proc/ProgramNanites(swarm, program)
        // Program the nanites